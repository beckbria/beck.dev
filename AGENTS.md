* When running on Windows, prefer to execute commands on Windows Subsystem for Linux via wsl.exe rather than locally on Windows
* All project dependencies should be managed and included through the Docker file
* When adding new dependencies, update README.md and the Docker configuration
